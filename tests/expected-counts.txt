# tests/expected-counts.txt — floor on the number of tests each binary must run.
#
# Why this file exists
# -------------------
# run-qt-tests.sh already refuses a binary that produces no successful test body
# at all. That catches a suite reduced to nothing; it does not catch a suite
# reduced from 62 tests to 1, which stays green.
#
# That is not hypothetical. A stale in-tree `.moc` — left by an earlier in-tree
# build, and picked up in preference to the freshly generated one because the
# compiler resolves `#include "tst_X.moc"` from the source directory first — was
# found to hide a newly added test: the binary embedded an out-of-date
# metaobject, reported one test fewer, and the suite passed. Two tests were
# masked that way, in tst_CliParser and tst_UpdateChecker.
#
# Format
# ------
#   <binary-name> <minimum-passed> [platform]
#
# `platform` is the value of `uname -s`, normalised: Linux, Darwin, Windows.
# A line without a platform applies everywhere. The most specific match wins.
# `minimum-passed` counts the `N passed` of the QtTest summary, data-driven rows
# included — a test that turns into a skip lowers it too, which is the point.
#
# It is a FLOOR, not an exact count: adding tests never requires touching this
# file. Only removing them does, which is precisely the decision worth making
# explicit.
#
# A binary with no applicable line is reported as unpinned and does NOT fail the
# run. Each floor is measured on its named platform; unmeasured combinations
# remain unpinned.
#
# Where these numbers come from
# -----------------------------
# Linux measured 2026-09-14 (Qt 6.11.1, NGPOST_BIN set), refreshed 2026-09-17
# from CI run 2f55212 — the three suites that had grown well past their floor
# (tst_MainWindow 72 -> 140, tst_PostFlow 16 -> 22, tst_Par2Settings 10 -> 17).
# Darwin and Windows come from that same run: macos-15 and windows-latest, Qt
# 6.8.2, where the Windows job runs elevated enough for the ACL fixtures.
#
# The three platforms do not run the same number of tests, and the gaps are
# expected: no GPU on the runners, no symlink privilege on Windows, the
# Windows-only suites that QSKIP everywhere else. Each column is what that
# platform actually ran, so a floor never asserts a number measured elsewhere.
#
# These figures assume the par2 tools CI fetches before the suites
# (.github/scripts/fetch-par2-tools.sh). Without them, the cases that run a real
# engine skip instead: tst_Par2Settings reports 15 rather than 17 on Linux. Fetch
# the tools before reading a local failure as a lost test.

tst_CliParser                 68  Linux
tst_FoldersMonitor            10  Linux
tst_GoldenNzb                  4  Linux
tst_MainWindow               203  Linux
tst_MockNntp                   8  Linux
tst_NntpFile                  29  Linux
tst_OpenVpnConfigPolicy       74  Linux
tst_Par2Settings              17  Linux
tst_PathHelper                45  Linux
tst_PostFlow                  28  Linux
tst_PostHistory               48  Linux
tst_PostInfoTemplate          36  Linux
tst_RandomToken                3  Linux
tst_SecretMasker              22  Linux
tst_UpdateChecker             35  Linux
tst_VpnDnsResolver             8  Linux
tst_VpnE2E_WireGuard           7  Linux
tst_VpnProfile                62  Linux
tst_VpnSocketBinder            8  Linux
tst_WindowsCommandLine         3  Linux
tst_WindowsSecurity            5  Linux
tst_WindowsServiceControl      5  Linux
tst_WireGuardBackend          13  Linux
tst_WireGuardConfigPolicy     21  Linux
tst_Yenc                      30  Linux

# Quick Post controls/history review: Linux and Windows GUI floors refreshed on 2026-09-22.

# macOS, measured 2026-09-17 on macos-15 / Qt 6.8.2.
# Lower than Linux where the platform decides: tst_CliParser skips the five
# cases that isolate the Qt TLS plugins with bwrap, tst_MainWindow skips the
# GPU probe and the bundled-tool case, tst_VpnProfile skips one install check.
tst_CliParser                 63  Darwin
tst_FoldersMonitor            10  Darwin
tst_GoldenNzb                  4  Darwin
tst_MainWindow               157  Darwin
tst_MockNntp                   8  Darwin
tst_NntpFile                  29  Darwin
tst_OpenVpnConfigPolicy       74  Darwin
tst_Par2Settings              17  Darwin
tst_PathHelper                45  Darwin
tst_PostFlow                  24  Darwin
tst_PostHistory               48  Darwin
tst_PostInfoTemplate          36  Darwin
tst_RandomToken                3  Darwin
tst_SecretMasker              22  Darwin
tst_UpdateChecker             35  Darwin
tst_VpnDnsResolver             8  Darwin
tst_VpnProfile                61  Darwin
tst_VpnSocketBinder            8  Darwin
tst_WindowsCommandLine         3  Darwin
tst_WindowsSecurity            5  Darwin
tst_WindowsServiceControl      5  Darwin
tst_WireGuardBackend          13  Darwin
tst_WireGuardConfigPolicy     21  Darwin
tst_Yenc                      30  Darwin

# Windows, measured 2026-09-17 on windows-latest / Qt 6.8.2 (MSVC).
# The Windows-only suites run here and only here; tst_PathHelper and
# tst_PostInfoTemplate skip the cases that need a symlink privilege, and
# tst_MainWindow skips the tool-path cases that need a POSIX file mode.
tst_CliParser                 65  Windows
tst_FoldersMonitor            10  Windows
tst_GoldenNzb                  4  Windows
tst_MainWindow               181  Windows
tst_MockNntp                   8  Windows
tst_NntpFile                  29  Windows
tst_OpenVpnConfigPolicy       74  Windows
tst_Par2Settings              18  Windows
tst_PathHelper                38  Windows
tst_PostFlow                  18  Windows
tst_PostHistory               48  Windows
tst_PostInfoTemplate          34  Windows
tst_RandomToken                3  Windows
tst_SecretMasker              22  Windows
tst_UpdateChecker             35  Windows
tst_VpnDnsResolver             8  Windows
tst_VpnProfile                62  Windows
tst_VpnSocketBinder            8  Windows
tst_WindowsBindHelper          9  Windows
tst_WindowsCommandLine        11  Windows
tst_WindowsSecurity           19  Windows
tst_WindowsServiceControl      6  Windows
tst_WireGuardBackend          13  Windows
tst_WireGuardConfigPolicy     21  Windows
tst_Yenc                      30  Windows

# Deliberately unpinned
# ---------------------
#   tst_WindowsBindHelper   Off Windows, every slot QSKIPs and the binary runs
#                           0 tests; the harness allowlists that empty suite.
#                           Pinned on Windows, where the 9 tests really run.
#   tst_VpnE2E_WireGuard    Built and run on Linux only (root, iproute2 and the
#                           privileged helper), so it has a Linux floor and no
#                           line for the other two platforms.

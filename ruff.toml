# The rules the CI enforces (main.yml, job "lint"): syntax errors, undefined
# names, unused imports and statement layout -- ruff's long-standing default.
# Pinned here because newer ruff releases widen the default set on their own.
[lint]
select = ["E4", "E7", "E9", "F"]
